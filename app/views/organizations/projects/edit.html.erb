<%= render 'projects/project_nav', organization: @organization, project: @project %>
<section class="causes-section bg-grey bd-bottom pt-4 pb-4">
  <div class="container">
    <div class="row">
      <div class='col-md-8 offset-md-2'>
        <div class='card card-body'>
          <%= render 'form', project: @project, form_url: organization_project_path(@organization["id"], @project["id"]), method: :put %>
        </div>
      </div>
    </div>
  </div>
</section>
